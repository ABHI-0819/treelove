import 'package:flutter/material.dart';

import 'package:flutter/material.dart';

class AppColor {
  AppColor._();

  // Core Colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF070707);

  // Primary & Secondary
  static const Color primary = Color(0xFF00473C); // Deep Forest Green
  static const Color primaryDark = Color(0xFF002D26);
  static const Color primaryLight = Color(0xFF1C665A);

  static const Color secondary = Color(0xFF63B27F); // Muted leafy green
  static const Color secondaryLight = Color(0xFF9DD9A5);
  static const Color secondaryDark = Color(0xFF387A58);

  // Backgrounds
  static const Color background = Color(0xFFF8F4E3); // Soft Creamy Beige
  static const Color scaffoldBackground = Color(0xFFF8F4E3);
  static const Color cardBackground = Color(0xFFFFFFFF); //

  // Text
  static const Color textPrimary = Color(0xFF1B1B1B); // Dark neutral
  static const Color textSecondary = Color(0xFF4F4F4F);
  static const Color textMuted = Color(0xFF8D8D8D);

  // Accent
  static const Color accent = Color(0xFFFFB703); // Warm yellow for highlights
  static const Color accentLight = Color(0xFFFFD95A);
  static const Color skyBlue = Color(0xFF8ECAE6); // Optional

  // Error
  static const Color error = Color(0xFFD32F2F);
  static const Color errorLight = Color(0xFFFFCDD2);

  // Borders and Dividers
  static const Color border = Color(0xFFDDDDDD);
  static const Color divider = Color(0xFFEAEAEA);

  // Status
  static const Color success = Color(0xFF2E7D32); // Verified
  static const Color warning = Color(0xFFF9A825); // Caution
}
